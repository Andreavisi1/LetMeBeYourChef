<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/btnHome"
            android:icon="@drawable/ic_map"
            android:title="Map" />
        <item
            android:id="@+id/btnSetting"
            android:icon="@drawable/ic_rating"
            android:title="Rate Places" />
        <item
            android:id="@+id/btnSavedPlaces"
            android:icon="@drawable/ic_location"
            android:title="Saved Places" />
        <item
            android:id="@+id/btnOptRoutes"
            android:icon="@drawable/ic_stonks"
            android:title="Optimize your routes" />

    </group>

    <item android:title="">
        <menu>
            <item
                android:id="@+id/btnHelp"
                android:icon="@drawable/ic_work"
                android:ellipsize="marquee"
                android:marqueeRepeatLimit="marquee_forever"
                android:scrollHorizontally="true"
                android:title="Work in progress..." />
            <item
                android:ellipsize="marquee"
                android:marqueeRepeatLimit="marquee_forever"
                android:scrollHorizontally="true"
                android:title="And always more!" />
        </menu>
    </item>
</menu>